<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="android:tabWidgetStyle">@drawable/apptheme_tab_indicator_holo</item> -->
    </style>
    <style name="noActionBar" parent="AppTheme">
        <item name="android:windowActionBar">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Run_ButtonDialog" parent="@android:style/Theme.Holo.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:borderlessButtonStyle">@style/Run_ButtonDialog_Text</item>
    </style>

    <style name="Run_ButtonDialog_Text">
        <item name="android:textSize">@dimen/dialog_button_text_size</item>
        <item name="android:textColor">@color/Text_ButtonDialog</item>
        <item name="android:background">@drawable/button_dialog</item>
        <item name="android:textStyle">bold</item>
    </style>




</resources>
